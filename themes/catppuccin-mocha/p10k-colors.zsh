# Catppuccin Mocha — P10k color overlay
# This theme uses the default p10k rainbow colors.
# The Catppuccin palette is applied via the terminal (Ghostty) theme,
# which remaps the ANSI color indices to Catppuccin values.
# No overrides needed — the base ~/.p10k.zsh colors work as-is.
