---
name: resume
description: Alias for /focus — load context for a workstream. Prefer /focus instead.
argument-hint: "<what you want to work on>"
user-invocable: true
allowed-tools: "Read Glob Grep Bash(cat:*) Bash(jq:*) Bash(ls:*) Bash(git log:*) Bash(git branch:*)"
---

# Resume Work

This is an alias for `/focus`. Run the same logic as the focus skill using `$ARGUMENTS` as the topic description.

Read `~/.claude/skills/focus/SKILL.md` and follow its instructions with `$ARGUMENTS` as the input.
